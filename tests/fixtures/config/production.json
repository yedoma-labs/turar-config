{
	"database": {
		"host": "${DB_HOST}",
		"port": 5432,
		"name": "prod_db",
		"pool": {
			"min": 10,
			"max": 100
		}
	},
	"server": {
		"port": 8080,
		"host": "0.0.0.0"
	},
	"logging": {
		"level": "warn",
		"format": "json"
	},
	"features": {
		"enableNewUI": true
	},
	"secrets": {
		"apiKey": "${API_KEY}"
	}
}
